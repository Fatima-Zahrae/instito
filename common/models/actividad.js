'use strict';

module.exports = function(Actividad) {  
    
    
    
    
     /*Actividad.beforeRemote('create', function (context, actividad, next) {
        context.args.data.owner = context.req.accessToken.userId;
         console.log(context.req.accessToken.userId);
        next();
    });*/
};
